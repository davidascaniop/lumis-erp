import { LoginForm } from "@/components/auth/login-form"

export const metadata = {
    title: "Iniciar Sesión — LUMIS",
    description: "Ingresa a tu cuenta de LUMIS ERP/CRM",
}

export default function LoginPage() {
    return <LoginForm />
}
